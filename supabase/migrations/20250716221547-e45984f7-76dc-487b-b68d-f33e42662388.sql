-- Check if we have the FIRECRAWL_API_KEY secret properly configured
-- This is just a placeholder query to trigger the secret form
SELECT 'Please configure FIRECRAWL_API_KEY in Supabase secrets' as message;